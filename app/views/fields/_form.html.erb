<%= simple_form_for [@event, @field],
	:html => { :class => 'form-horizontal' } do |f| %>
  <%= f.error_notification %>

  <%= f.input :field_label, :label => "Tittel" %>
  <%= f.input :required, :label => "Må utfylles" %>
  <%= f.input :field_type_id, :collection => FieldType.all, :label => "Type" %>
  <%= f.input :event_id, :as => :hidden	 %>
  <%= f.input :field_order, :as => :hidden %>
  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to 'Cancel', edit_event_path(@event), :class => 'btn' %>
  </div>
<% end %>

<h2><%= t ".open-events", default: "Open registrations" %></h2>
<ul>
	<% @events.each do |event| %>
		<li>
			<h3>
				<a href="<%= reply_event_path(event) %>"><%= event.title %></a>
			</h3>
		</li>
	<% end %>
</ul>
<h1>Listing replies</h1>

<table class="table table-striped">
  <thead>
    <tr>
      <th>Navn</th>
      <th>Påmeldte</th>
      <th>Handlinger</th>
    </tr>
  </thead>

<% @events.each do |event| %>
    <tr>
      <td><%= event.title %></td>
      <td><%= event.replies_count %>/<%= event.max_att %></td>
      <td><%= link_to 'Show', replies_event_path(event) %> | <%= link_to 'Export to excel', replies_event_path(event, format: "xls") %> 
      <%# link_to 'Export to csv', replies_event_path(event, format: "csv") %> </td>
      <td><%# link_to 'Destroy', event, confirm: 'Are you sure?', method: :delete %></td>
    </tr>
<% end %>
</table>

<br />

<%= link_to 'New Reply', new_reply_path %>
